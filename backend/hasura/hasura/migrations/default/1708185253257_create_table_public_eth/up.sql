CREATE TABLE "public"."eth" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "clientId" uuid NOT NULL, "walletId" uuid NOT NULL, "sepolia" text NOT NULL, "mainnet" text NOT NULL, "ropsten" text NOT NULL, "rinkeby" text NOT NULL, "goerli" text NOT NULL, "kovan" text NOT NULL, "sepoliaEth" float8 NOT NULL DEFAULT 0.00, "mainnetEth" float8 NOT NULL DEFAULT 0.00, "ropstenEth" float8 NOT NULL DEFAULT 0.00, "rinkebyEth" float8 NOT NULL DEFAULT 0.00, "goerliEth" float8 NOT NULL DEFAULT 0.00, "kovanEth" float8 NOT NULL DEFAULT 0.00, PRIMARY KEY ("id") , FOREIGN KEY ("clientId") REFERENCES "public"."client"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("walletId") REFERENCES "public"."wallet"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("clientId"), UNIQUE ("walletId"), UNIQUE ("sepolia"), UNIQUE ("mainnet"), UNIQUE ("ropsten"), UNIQUE ("rinkeby"), UNIQUE ("goerli"), UNIQUE ("kovan"));COMMENT ON TABLE "public"."eth" IS E'eth address and token for client wallets';
CREATE EXTENSION IF NOT EXISTS pgcrypto;

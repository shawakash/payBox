table:
  name: wallet
  schema: public
object_relationships:
  - name: client
    using:
      foreign_key_constraint_on: clientId
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: walletId
        table:
          name: account
          schema: public

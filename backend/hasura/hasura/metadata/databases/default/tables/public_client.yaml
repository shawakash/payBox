table:
  name: client
  schema: public
object_relationships:
  - name: address
    using:
      foreign_key_constraint_on:
        column: client_id
        table:
          name: address
          schema: public

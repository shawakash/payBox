table:
  name: client
  schema: public
object_relationships:
  - name: chain
    using:
      foreign_key_constraint_on:
        column: client_id
        table:
          name: chain
          schema: public

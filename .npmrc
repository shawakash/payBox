# Set the registry URL for npm
registry=https://registry.npmjs.org/

# Set the authentication token for the registry
//registry.npmjs.org/:_authToken=YOUR_AUTH_TOKEN

# Set the proxy URL for npm
proxy=http://your-proxy-url:8080

# Set the HTTPS proxy URL for npm
https-proxy=http://your-https-proxy-url:8080
